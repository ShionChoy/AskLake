# Copy to auth.yaml (gitignored) and replace the example tokens.
# Each role MUST be one of the roles declared in datasets/<name>/governance.yaml.
tokens:
  replace-with-analyst-token: {user: alice, role: analyst}
  replace-with-public-token: {user: viewer, role: public}
